# Deployment

Primary production path: Render Blueprint in the repository root (`render.yaml`).
Fallback API deployment path: Railway configuration in the repository root (`railway.json`).

See `docs/deployment-render.md` for Render setup and verification steps, and `docs/deployment-status.md` for the current hosting status.
